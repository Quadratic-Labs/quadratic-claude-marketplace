---
description: Smart PR creation and updates with auto-generated descriptions
argument-hint: Optional PR title or description
allowed-tools: ["Skill"]
---

# DevKit Init PR

Invoke the `/pr` skill to run the smart PR workflow.

## Your Task

Use the Skill tool to trigger the pr skill:

```
Skill tool:
  skill: "pr"
  args: "$ARGUMENTS"
```

**User arguments:** $ARGUMENTS
