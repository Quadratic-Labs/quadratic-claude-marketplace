---
description: Interactive release workflow with automated checks and tasks
argument-hint: Optional version number or release type
allowed-tools: ["Skill"]
---

# DevKit Init Release

Invoke the `/release` skill to run the interactive release workflow.

## Your Task

Use the Skill tool to trigger the release skill:

```
Skill tool:
  skill: "release"
  args: "$ARGUMENTS"
```

**User arguments:** $ARGUMENTS
